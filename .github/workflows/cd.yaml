permissions:
  contents: write
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - 'k8s/**'
      - 'README.md'
      - '.github/**'
      - 'scripts/**'
env:
  IMAGE: version-promote
  BOT_USER: "github-actions[bot]"
  BOT_EMAIL: "bot@github.com"
jobs:
  update-image-tags:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Update the image tag in dev
        run: |
          HASH="$(./scripts/get-current-commit-hash)"
          ./scripts/update-image-tag dev $IMAGE "$HASH"
      - name: Commit and push to main
        run: |
          git config user.name $BOT_USER
          git config user.email $BOT_EMAIL
          git commit -a -m "Update image tag in dev"
          git pull --rebase
          git push
      - name: Update the image tag in prod
        run: |
          HASH="$(./scripts/get-current-commit-hash)"
          ./scripts/update-image-tag prod $IMAGE "$HASH"
      - name: Create PR
        env:
          BRANCH_NAME: update-prod-tag
          GH_TOKEN: ${{ github.token }}
        run: |
          git config user.name $BOT_USER
          git config user.email $BOT_EMAIL
          git checkout -b $BRANCH_NAME
          git commit -a -m "Update image tag in prod"
          git pull --rebase
          git push --set-upstream origin $BRANCH_NAME
          gh pr create --fill
